// Numbers in between range
function numbersInRange(num1, num2) {
  let temp = [];
  //   if (num1 + 1 === num2) {
  //     return temp.push(num2);
  //   }
  temp.push(num1 + 1);
}

/@TODO: implement the logic with recursion as its yet not completed  /;
// console.log(numbersInRange(2, 5));
